name: keep-render-awake
on:
  schedule:
    - cron: '*/15 * * * *'   # every 15 minutes
  workflow_dispatch:        # 👈 manual trigger
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Curl app
        run: curl -fsS https://ai-chart-intelligence-tool.onrender.com/health || true
